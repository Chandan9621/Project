import { CommaBeforeISTPipe } from './comma-before-ist.pipe';

describe('CommaBeforeISTPipe', () => {
  it('create an instance', () => {
    const pipe = new CommaBeforeISTPipe();
    expect(pipe).toBeTruthy();
  });
});
